<?php


class Config
{
    protected static $config  = [
        'servername' => 'localhost',
        'username' => 'user',
        'password' => 'password',
        'dbname' => 'database'
    ];

    public static function getConfig()
    {
        return self::$config;
    }
}
